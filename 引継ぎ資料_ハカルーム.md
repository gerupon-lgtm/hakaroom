# ハカルーム 引継ぎ資料

作成日: 2026-09-26 / 時点のバージョン: `hakaroom-0.8.2` / 作成者: Claude Code（Claude Sonnet 5）

## 0. この資料の使い方

このプロジェクトを引き継ぐ別のAIエージェント（Codex等）向けの資料。最初に「1. 依頼者と進め方」「7. 未解決の論点」を読み、要件は `要件定義書_ハカルーム.md` を正とする。実装の詳細は `docs/implementation-guide.md`。

## 1. 依頼者と進め方（重要）

- 依頼者は個人（シクミラボ、SIKUMILABブランド）。**開発者本人がPixel 6a／Android版Chromeで実機検証**している。スクリーンショットを貼って結果を報告してくる
- **目的は「スマートフォンでどこまでできるかを調べる個人検証」。** 精度の±3〜5cmは**目標値であり、合否判定ではない**（要件定義書9章・17章で確定）。誤差は「合格／不合格」ではなく、大きさと傾向（平均・ばらつき・最大）として扱う。「±3cm超」を失敗のように書かないこと
- **勝手に検証対象から外さない。** 依頼者が「試したい」と言ったものを、こちらの判断で除外しない（過去に指摘を受けた）
- 説明は**平易な日本語で短く**。専門用語や推測を並べると「分かりづらい」と言われる。**白黒・低解像度のスクリーンショットから物の種類を断定しない**（過去に「布団が写っている」と誤って断定して指摘を受けた）。分からないことは質問する
- 要件・スコープ・優先度の変更は、依頼者の合意を取ってから要件定義書（改訂履歴つき）に反映する。0章のA/B/C区分に従う
- **デプロイのたびに `APP_VERSION` を上げる**（依頼者が「更新されたかフッタで確認する」ため）。`src/version.ts`（正典）、`package.json` の `version`、`docs/implementation-guide.md` の現在版の3か所を必ず揃える。バージョン形式は `hakaroom-MAJOR.MINOR.PATCH`
- 「進めますか」と聞いて依頼者が選ぶ形が好まれる。選択肢は2〜3個に絞り、推奨を明示する
- 「保留の場合は開発を止める」という依頼者の方針がある（AR方式の精度が出なければ保留、と明言済み）。写真方式で精度が出ない場合の扱いは、まだ決まっていない

## 2. プロジェクト概要

スマートフォンのブラウザだけで、部屋の角を指定して壁長・面積・簡易平面図を出せるかを検証するWebアプリ（正式名称「ハカルーム」）。サーバーを持たず、データは端末内に閉じる（要件定義書 C-01）。

- リポジトリ: https://github.com/gerupon-lgtm/hakaroom（Public。`gh` は `gerupon-lgtm` で認証済み）
- 公開URL（実機確認用）: https://gerupon-lgtm.github.io/hakaroom/
- ローカル: `C:\Users\user\Documents\project\ハカルーム`（Windows 11 / Git Bash。Node 24）
- デプロイ: `main` へのpushで GitHub Actions（`.github/workflows/deploy.yml`）が typecheck→テスト→ビルド→GitHub Pages。約45秒
- 技術: Vite + TypeScript（フレームワークなし）、Vitest、three.js（動的import）、IndexedDB（写真）

## 3. ドキュメントの状態

| ファイル | 状態 |
|---|---|
| `要件定義書_ハカルーム.md` | **v0.4（最新）**。F-001〜F-026。写真方式・折れ線入力を追加済み。改訂履歴あり |
| `構想たたき台_ハカルーム.md` | 元資料（参考） |
| `基本設計サマリ.md`、`docs/screens.md`、`docs/api-design.md`、`docs/tasks.md` | **AR方式中心の古い版。写真方式（F-021〜F-026）は未設計**（tasks.mdに「未設計」の行だけ追加済み） |
| `docs/data-model.md` | PhotoRecord（写真）は追記済み。それ以外は設計のみで**未実装**（localStorageのStorageRootは実装していない） |
| `docs/implementation-guide.md` | 実装規約、変更禁止事項、検証済みの事実（`window.prompt` がARを終了させる件のみ記載）。バージョン現在版はここにも書く |
| `CLAUDE.md` / `AGENTS.md` | 入口。共通本文は implementation-guide.md |

### 要件定義書の主な確定事項

- 対象外: iPhone対応、アカウント機能。Pixel 6a／Android Chrome専用
- C-01: 検証データ（写真含む）はサーバーへ送らず端末内で完結。C-02: 生値・補正値・手入力値を混在させない。C-03: 自動検出結果は利用者が確認・修正してから確定
- 写真は端末内（IndexedDB）に、**解析用に縮小して保存**（既定: 長辺1600px・グレースケール・WebP品質0.6、実測で1枚146KB）。合計上限200MB、超えたら古い写真から削除
- AR方式は「保留」（削除はしない）
- 開発標準: 保存はUTC・表示は端末TZ（フォールバックJST）、バージョン形式、SIKUMILABのフッタ表示（`hakaroom-x.y.z` と `© 2026 SIKUMI LAB`）

## 4. コードの構成

```
src/main.ts                       起点。環境チェック→技術検証画面
src/version.ts                    APP_VERSION（正典）
src/types/index.ts                共通型（PhotoRecord, TiltInfo 等）
src/ar/                           AR方式（WebXR）
  check-availability.ts           WebXR対応判定
  with-timeout.ts                 初期化のタイムアウト
  webxr-native/two-point-prototype.ts   素のWebXRの2点計測（基準距離による縮尺補正つき）
  threejs/two-point-prototype.ts        Three.js版（タップ即記録）
src/geometry/                     距離・面積（純粋関数）
src/photo/                        写真方式
  tilt.ts / tilt-sensor.ts        重力ベクトル→仰角・ロール（devicemotion）
  process-image.ts / image-size.ts  縮小・グレースケール・WebP
  rectify.ts                      幾何計算（傾き+焦点距離の水平面射影、A4のホモグラフィ、焦点距離推定、鉛直ガイド）
src/storage/photo-store.ts        IndexedDB（写真）。photo-eviction.ts は上限超過の削除判定
src/screens/                      environment-check / tech-validation（入口） / photo-lab（撮影） / photo-measure（写真上で測る）
tests/                            geometry / check-availability / photo / rectify（計39件）
```

技術検証画面（`tech-validation.ts`）から各プロトタイプに入る。**正式な画面（S-001〜S-007）は未実装**で、現状は検証ツールの集まり。

## 5. 実機検証で分かったこと

### 5.1 AR方式（WebXR hit-test。Pixel 6a）

- 同じ3.58m区間の2点計測は、誤差が-53cm〜+1.5cmで大きくばらつく。**一貫して実際より短く**出る傾向があり、同一セッションで繰り返すと縮むが、-15〜-20cm付近で頭打ち
- `local` と `local-floor`、素のWebXRとThree.js、操作方法（クロスヘア＋ボタン／タップ即記録）を変えても傾向は同じ。`local-floor` では同じ高さの床の角でも高さ差の警告が出る
- **「基準距離による縮尺補正」（v0.5.0）は実装済みだが、依頼者はまだ検証していない**（未確認）
- 罠: 立ち上げ直しではトラッキングが初期化される／`window.prompt()` はARセッションを強制終了させる（入力欄はDOM Overlay内に置く）／Androidの全画面案内バナーが下部に出て、下端のボタンと重なる／`select` イベントとボタンの `click` が二重に発火する（`beforexrselect` で抑止）

### 5.2 写真方式（v0.6〜0.8）

- **傾きセンサー**（devicemotion）は Pixel 6a / Chrome で取得できた（依頼者確認）。写真は1枚146KB
- **方式B（A4の四隅＋ホモグラフィ）** の実測（すべて依頼者の手指定。誤差は実測との差）:
  - 壁面（キャビネット下辺）0.91m → 0.891m（-2.1%）
  - 壁面 2.63m → 2.552 / 2.652 / 2.738 / 2.75m（-3.0〜+4.8%）。**同じ辺でも四隅の指定し直しで約±10cm変わる**（A4が写真内で小さく、辺の長さの約9倍に引き伸ばされるため）
  - 床（A4を寝かせた、仰角-30.5°）1.42m → 1.434m（+1.0%）
  - 床（仰角-25.7°、線分3本）1.42m → 1.568m（+10.4%）、0.25m → 0.214m（-14%）、0.91m → 0.596m（-35%）。**原因は未特定**。依頼者は「床に接する部分を指定した」と説明。仮説は「床を浅い角度で見て、遠い位置ほど1ピクセルの誤差が大きく効く」「A4と点が同じ面にない」だが、いずれも未検証
- **方式A 初の実データ（v0.8.3、上の床写真・横持ち・基準0.91m）**: 1.42m → 1.644m（+15.8%）、0.25m → 0.252m（+0.9%）。同じ写真の方式B（A4指定し直し）: 1.42m → 1.457m（+2.6%）、0.25m → 0.280m（+11.8%）、0.91m → 1.053m（+15.7%）。どちらも「基準の近くは合い、離れると外れる」傾向。v0.8.2までは横持ちの回転符号が逆で、方式Aは -68% 等になっていた（修正済み）。点を読み取った再計算では、ロール2.4°で約8%、仰角1°で約2.5%結果が動く（ロールが約95°なら1.42mが合う）。原因（角度の誤差か、点が床と同じ高さにないか）は未特定
- **v0.9.0: 縦線2本（柱・戸の縁）から傾きを求める機能と、縦線＋センサーから焦点距離を推定する機能を追加**（依頼者の選択）。依頼者のスクリーンショットから読んだ点での試算では、焦点距離26mmだと縦線の傾き（仰角-15.6°）がセンサー（-25.7°）と合わず、**約44mmで両者が一致し、1.42mも約1.37〜1.40mに合う**。既定の26mmが実際と違う可能性が高い（未確認。実機で推定ボタンの結果を見る）。依頼者の方針: 長い距離をメジャーで測る検証は目的（小さな基準で大きな距離を測る）に反するので求めない。既に実測済みの部屋の長さで確認する
- **v0.9.0 実機結果（同じ床写真、依頼者の手指定）**: 縦線＋センサーで焦点距離 **32.1mm** と推定（向きの差0.51°。上の試算の約44mmは読み取りが粗く外れていた）。基準0.91mで、センサー: 1.42m→1.455m（+2.5%）、0.25m→0.245m（-2.2%）／縦線: 1.42m→1.435m（+1.0%）、0.25m→0.247m（-1.4%）。32.1mmが他の写真でも通用するかは未確認
- **焦点距離32mmを別の写真に適用（v0.9.0、縦持ち・仰角-28.9°、床）**: 基準0.297m（A4の長辺）だけで、2.365m→2.410m（+4.5cm、+1.9%）、2.455m→2.476m（+2.1cm、+0.8%）。「小さな基準で長い距離」がこの写真では成立。32mmが縦持ち・横持ちの両方で通用した（写真2枚）
- 方式Aは「基準と測定が同じ水平面（床）で、カメラより低い」ときだけ計算できる。カメラと同じ高さの壁の線では表が「—」になる（依頼者が壁写真で遭遇。理由表示の追加は依頼者が不要と回答）
- 依頼者から「説明がすごくわかりづらい」と指摘あり（v0.9.0の操作説明）。操作説明は手順を番号で短く、専門用語（仰角・ロール等）を避ける
- 方式Aの補足（v0.8.2以前の記述）: 合成データでの往復は単体テストで確認済み。焦点距離の既定値26mm（35mm換算）はPixel 6aの実値未確認で、推定機能（基準2本以上）がある。表の「種別」を切り替えて、基準1本＋測定で比較できる（v0.8.2）
- 傾き補正の**画像回転（0/90/180/270）の符号は、縦持ち（0）でだけ鉛直ガイド線で確認済み**。横持ち（回転90）の符号は未確認
- 家の中では、画角の制約で長い辺（3m超）が1枚に収まりにくい

## 6. 写真ツールの操作（現状）

- 撮影: 技術検証D（撮影）で、縦持ちのアプリ内カメラ。傾き・画面の向き・焦点距離（任意入力）を保存
- 測定: 技術検証D（写真上で測る）。タップで照準を置き、ドラッグは指の動きの0.4倍で微調整、「確定」「取消」の固定ボタン。置いた点は近くをタップして動かし直せる。「直前を取り消す」あり。A4は「最初の辺」を画像上の長さで自動判定
- **測定結果（線分・A4）は端末に保存していない（画面を離れると消える）。** 検証記録（F-008）との連携は未実装

## 7. 未解決の論点・次にやること候補

依頼者はまだ次の方針を選んでいない。優先度は依頼者と相談すること。

1. **床の写真で方式A と方式B を比べる**（線分1を「基準」に切り替えて、2・3の誤差を見る）。上の +10%／-35% の原因を切り分ける
2. **A4の縁を自動検出して四隅を細かく直す**（提案済み・未着手）。利用者が確認して確定する（C-03）。指定のばらつきの低減が狙い
3. **カメラ選択（超広角レンズ）**（提案済み・未着手）。歪み補正が必要になる可能性
4. **長い辺の分割撮影・写真のつなぎ（F-024）と折れ線入力（F-026）** は未実装（家の中の画角制約への現実解）
5. 結果表示の調整（合意済み・未実装）: 技術検証画面の「±3cm以内／超」の赤緑バッジをやめ、誤差（cm・%）のみ、平均・ばらつき・最大誤差の集計を出す
6. 写真方式の設計書（screens／api-design／tasks）と基本設計サマリを、F-021〜F-026に合わせて更新
7. 要件定義書 20章の未解決: No.1（実用基準は検証後）、No.3（WebXR挙動）、No.9（基準物の複数配置の統合）、No.10（保持上限の具体値）、No.12（折れ線と写真の混在）、No.13（床以外の面の適用条件）など
8. 写真方式で精度が出ない場合の扱い（保留か、折れ線入力を主にするか）を、依頼者と決める

## 8. 開発の実務メモ

- 検証: `npm run typecheck` / `npm test` / `npm run build`。デプロイ完了は `gh run list --repo gerupon-lgtm/hakaroom --limit 1` で確認（pushの直後は前回の実行が返るので数秒待つ）
- **Git Bashでの複雑なheredoc（日本語やクォートを含む）は壊れやすい。** 大きな編集は、スクリプトファイルに書いてから実行するか、エディタ系ツールで編集する
- `git` のCRLF警告は無害。コミットメッセージは日本語、末尾に共著の行（環境の指示に従う）
- 新しい実機検証機能を足すときは、まず**依頼者が何を確認したいか**を1〜2行で言い直してから作ると、認識のずれが減る
- 「変更禁止事項」「検証済みの事実」は `docs/implementation-guide.md` に、根拠つきで追記していく運用
