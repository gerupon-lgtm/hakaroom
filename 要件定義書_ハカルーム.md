# ハカルーム 要件定義書

## 0. この文書の運用ルール（実装者・AIエージェント向け）

**この要件定義書は、実装を止めるための禁止事項集ではない。** 発案者（依頼者本人）の指示によって変更できる範囲を、次の3段階で定める。実装中に発案者から要件と異なる明示的な指示が出た場合、A・Bに該当するものを「要件違反」として拒否しない。

| 区分 | 対象 | 扱い |
|---|---|---|
| **A：発案者の指示で即変更してよい** | 画面配置・レイアウト、UI文言、色・余白、ボタンやタブの位置、アイコン、一覧の既定の並び順、細かな操作導線、入力欄の見せ方 | その場で指示に従う。文書の更新は任意で、まとめて反映してよい。文書更新や再承認を着手条件にしない |
| **B：発案者の指示で覆してよいが、記録を残す** | 機能の追加・削除・優先度の変更、MVP範囲の増減、既定値、状態×操作の可否、棄却条件。ただしCに抵触しないもの | 指示に従って変更し、**改訂履歴に1行追記**して該当行を更新する。既に明示された指示の再承認は求めない |
| **C：この文書の改訂を経ずに覆さない** | 本章で列挙する「動かさない前提」 | 実装中の判断や単なる口頭の操作指示では外さず、発案者と具体的な改訂内容を合意して文書へ反映してから進める |

### Cに該当する「動かさない前提」

| ID | 動かさない前提（具体的な対象・条件） | 理由・保護するもの | 根拠・合意状況 |
|---|---|---|---|
| C-01 | 検証データ（測定値・環境条件・撮影画像等）は既定でサーバー送信・外部保存を行わず、端末内で完結させる | プライバシー保護 | 本document Q&A（非機能要件）で確定。将来の一般公開検討時は本書の改訂を経て見直す |
| C-02 | 生の計測値・補正値・手入力値は区別して保持し、後から値を上書き・混在させない | 設計の一貫性（精度検証という本アプリの目的の根幹） | 本document Q&A（データ要件）で確定 |
| C-03 | 基準物の自動検出結果は、利用者が輪郭・端点を確認・修正してからでなければ縮尺計算の確定値として使わない | 設計の一貫性（自動検出の誤りをそのまま採寸結果に反映させないため） | 本document Q&A（縮尺基準モード）で確定 |

### 指示と記録の扱い

- Cに該当しない範囲では発案者の裁量を優先する。A・Bの明示的な変更指示に、特別な「禁止解除」の宣言は不要。
- 表示の変更でも、C-01〜C-03へ実際に影響する場合はCとして扱う。反対に、すべての既定値や業務ルールを一律にCにしない。

---

## 1. 文書情報・改訂履歴

| 項目 | 内容 |
|------|------|
| 文書名 | ハカルーム 要件定義書 |
| システム・サービス名 | ハカルーム（**正式名称**。2026-09-23に構想段階の仮称から正式名称化を確認済み） |
| 版数 | 0.5 |
| 作成日 | 2026-09-23 |
| 作成者 | シクミラボ |
| 承認者 | シクミラボ（開発者本人が兼任）【想定】 |

改訂履歴（追記専用）：

| 版数 | 日付 | 変更内容 | 起点 | 変更者 |
|------|------|----------|------|--------|
| 0.1 | 2026-09-23 | 初版ドラフト。`構想たたき台_ハカルーム.md` を入力に、Q&Aで詳細化して作成 | — | シクミラボ |
| 0.2 | 2026-09-23 | ドラフトレビューを受け、設計に先立って詰めるべき要確認事項（ホスティング先、F-016の検証記録対象化、バッチ撮影の中断要否、Undo要否、検証記録の編集・削除可否、基準物の複数配置、データ保持方針）を確定し反映 | — | シクミラボ |
| 0.3 | 2026-09-25 | AR方式の実機検証（生の位置推定は縮尺が一貫して小さめに出て、繰り返しても収束しない）を受け、写真方式（D方式：アプリ内撮影＋傾きセンサー補正＋辺長／A4で縮尺付与）と折れ線入力（平板測量方式）を追加（F-021〜F-026）。精度目標を「±3〜5cm目標・合否判定としない・誤差と傾向を記録」に表現修正。AR方式は保留として残す | 発案者指示（区分B） | シクミラボ |
| 0.4 | 2026-09-25 | 写真方式の写真を端末内に保存する方針を確定（旧「画像は保存しない・メタデータのみ」を変更）。保存形式は解析用に縮小した画像（長辺1600px・グレースケール・WebP品質0.6を既定、検証中は設定変更可）、元画像は保存しない。写真の合計上限200MB、超過時は古い写真から削除し測定値の記録は残す。C-01（端末内完結）は維持 | 発案者指示（区分B）。旧記述「画像は保存しない」は本項で置換 | シクミラボ |
| 0.5 | 2026-09-26 | 写真方式（D方式）を**保留**とし、開発を停止。実機検証（v0.9.0）で、床の上の小さな基準（A4の長辺0.297m）から約2.4mを誤差2〜5cmで測れた（写真2枚）一方、測れるのは基準と同じ高さの水平面上の線に限られ、A4の4隅方式は基準が小さく写ると精度が出ない。発案者の判断：実用性に欠け、屋外では破綻する見込みのため、現仕様では使えない。削除はしない | 発案者判断（区分B） | シクミラボ |

---

## 2. はじめに（背景・目的）

### 背景

部屋の寸法や面積を知るには、通常メジャーやレーザー距離計で測り、数値をメモして図面に書き起こす。道具の準備・各辺の測定・数値の転記・面積計算が別々の作業になり、測り忘れや転記ミスが起こり得る。

### 目的

スマートフォンのブラウザ（WebXR）だけで、部屋の角を指定するだけの操作から採寸・面積計算・簡易図面化までを一続きにできるかを、実測との比較検証を通じて確認する。GPSやネイティブアプリのインストールを前提としない。

### 期待効果

- スマートフォンだけで、採寸・面積計算・簡易図面化を一続きにできる可能性
- GPSが届かない屋内でも利用でき、ネイティブアプリの配布が不要
- 計測部分と作図部分を分離し、将来計測方式を変更しても図面機能を活かせる

---

## 3. スコープ（対象範囲・対象外）

### 対象範囲

- 長方形の部屋の4点計測、壁長・面積・簡易平面図の表示、実測値との比較記録
- L字形など5点以上の多角形への対応（優先度は今後変動）
- 縮尺基準モード（基準なし／メジャー／登録済み基準物／任意基準物）
- WebXR測点方式（A）、固定地点重複撮影方式（B、リアルタイム・バッチ撮影の両方）、ハイブリッド方式（C）の比較検証
- 精度検証記録の保存・比較
- 保存・共有・出力機能（優先度は今後変動）
- 簡易2点間距離計測（部屋計測セッションとは別の軽量機能）
- **写真方式（D方式）**：アプリ内で複数枚撮影（傾きセンサー値を保存）し、写真間で同一の辺を指定、辺長入力またはA4等の目標物で縮尺を与えて平面図化する。ギャラリーからの取り込み（EXIFのみ）にも対応するが、アプリ内撮影を優先する（傾きセンサーが方式継続可否の鍵）
- **折れ線入力（平板測量方式）**：「起点から右／左へ○度折れ、○○m」を順に入力して平面図化する。写真のない部分の補完にも使う
- AR方式（A・C）は実機検証で生の位置推定に一貫した縮尺バイアスが確認されたため**保留**とし、削除はしない（基準距離による補正の検証は未実施）
- 写真方式（D方式）も、実機検証の結果（v0.5改訂履歴参照）を受けて**保留**とし、開発を停止する。削除はしない

初期利用者は開発者本人（Pixel 6a／Android版Chrome／HTTPS）。一般公開は将来判断とする。

### 対象外・保留

| 対象外・保留項目 | 理由 | 再検討条件 |
|---|---|---|
| iPhone対応 | 初期はAndroid（Pixel 6a）での精度検証を優先するため | Android実装が安定し、他端末への展開を検討する時 |
| アカウント機能 | 個人検証段階では不要なため | 一般公開・複数利用者対応を検討する時 |
| 写真方式（D方式）【保留】 | 測れるのが基準と同じ高さの水平面上の線に限られ、実用性に欠ける。屋外では破綻する見込み（発案者判断、2026-09-26） | 発案者が再開を指示した時 |

**注記**: 上記2項目以外（L字形対応、保存・共有・出力、一般公開）は対象範囲に含めるが、優先度は「あれば」「将来」の水準で今後変動しうる（発案者の明示指示による。区分Bの変更として扱う）。

---

## 4. 用語定義

| 用語 | 定義 |
|------|------|
| WebXR | ブラウザからAR/VR機能を利用するためのWeb標準API |
| ARCore | Googleが提供するAndroid向けAR基盤。WebXRの内部で利用される |
| Hit Test | カメラ映像から実世界の平面・点を検出し、3次元位置を推定する機能 |
| Anchor | WebXRで固定した3次元空間上の位置情報 |
| 測点 | 利用者が画面上で指定した、部屋の角などの記録済み座標点 |
| 計測セッション | AR開始から結果表示（または「ここで終了」操作）までの一連の計測の単位 |
| 縮尺基準 / 基準物 | 実寸との対応をつけるために利用する物（メジャー区間、登録済み基準物、任意の物） |
| 対応点 | 複数の画像間で同一の実世界の点として利用者が指定した点の組 |
| 閉合誤差 | 部屋を一周して測点を記録した際の、始点と終点のずれ。折れ線入力では、閉じる指定をした場合に「閉じるために必要な差（ずれの距離・方向）」として表示する（配分補正はしない【想定】） |
| 写真方式（D方式） | 撮影した複数枚の写真から、同一辺の指定・傾き補正・縮尺付与により床面（または目標物と同じ高さの平面）の寸法を求める方式 |
| 傾きセンサー | 端末の重力方向（ピッチ・ロール）を取得するセンサー（ブラウザのDeviceOrientation等）。アプリ内撮影時に写真と一緒に記録する |
| 折れ線入力 | 起点から「右／左へ○度折れ、○○m」を順に入力して形状を作る、平板測量で使われる方式 |
| 目標物 | 縮尺の基準にする既知寸法の物（A4用紙等）。測る辺と同じ高さの平面に置く前提 |
| 棄却条件 | その機能が不要と判断できる具体的な状態（8.2参照） |
| 優先度（核／検証／あれば／将来） | 核＝MVPの中心機能、検証＝精度・方式の検証を目的とする機能、あれば＝あると望ましい機能、将来＝後続フェーズで検討する機能。優先度は今後変動しうる（確定） |

---

## 5. 現状（As-Is）と理想（To-Be）

- **As-Is**: メジャー・レーザー距離計で各辺を測定 → 数値をメモ → 図面に書き起こし → 面積を計算。作業が分断されており、測り忘れ・転記ミスが起こり得る。
- **To-Be**: スマートフォンのブラウザで部屋の角を順に指定するだけで、四辺の長さ・面積・簡易平面図が自動的に得られ、実測値との差も記録できる。
- **ギャップ**: スマートフォンだけでどこまでの精度が出せるか（目標は壁1辺±3〜5cm程度。合否ではなく、できること・できないことを調べるのが目的）が最大の論点。AR位置推定単独では縮尺が小さめに出る傾向が確認されたため、写真方式・折れ線入力・基準物や幾何条件による補正を含めて比較する。

---

## 6. ステークホルダー・体制

| 役割 | 部門/氏名 | 責任 |
|------|-----------|------|
| 発案者・開発者・初期利用者・承認者 | シクミラボ（本人） | 要件確定、実装、精度検証、受入判断のすべてを兼任【想定】 |

個人検証プロジェクトのため、役割分離や複数ステークホルダーの調整は現時点で発生しない。一般公開時に再定義する。

---

## 7. 業務要件

### 7.1 初回価値シナリオ

| 対象利用者 | 開始状態 | 最初の価値 | 最短経路 | 必要な前提/初期付与 | 到達条件 | 前提不足時の誘導 |
|---|---|---|---|---|---|---|
| 開発者本人 | HTTPSで公開された検証ページをPixel 6aのChromeで開いた状態 | 長方形の部屋の四辺・面積・簡易平面図が表示され、実測値と比較できる | 対応環境チェック → AR開始・床認識 → 4点記録 → 結果表示 | Pixel 6a、最新版Android Chrome、Google Play開発者サービス（AR）、HTTPS、カメラ等の許可、位置推定に適した明るさ・周辺特徴 | 4点の記録が完了し、四辺・面積・簡易平面図が表示される | 非対応環境の場合、対応環境チェック画面で理由と対処法を案内する【想定】 |

### 7.2 主要リソースのライフサイクル

| リソース | 生成/取得 | 利用条件 | 更新 | 取消 | 上限 | 重複/競合 | 上限超過 | 消費/削除 | 復元/監査 |
|---|---|---|---|---|---|---|---|---|---|
| 測点 | AR中に利用者が画面上の角/点をタップして記録 | セッションが「記録中」状態であること | 「1点戻る」で直近点を取消、または任意の点を選択して削除・修正可能（記録中のみ） | 「1点戻る」（直近1点）または任意点の選択削除。取消後は後続の計算から除外 | 無制限（確定） | 同一位置への重複記録は許容し、利用者が削除操作で対応する【想定】 | 該当なし | セッション完了/破棄時にセッションへ従属して扱われる | Undo機能は無し。削除は取り消し不可（確定） |
| 計測セッション | AR開始操作で新規作成 | 対応環境チェックを通過していること | 測点の追加/削除、状態変化 | いつでも破棄可能。破棄後は測点・途中結果を保持しない | 同時に1セッションのみ【想定】（単一利用者・単一端末） | 該当なし | 該当なし | 「ここで終了」操作、または4点（L字は5点以上）の記録完了で完了状態へ | 追跡ロスト時は直前まで記録した測点を保持し、基準を取り直した上で再開できる（確定）。破棄後の復元は不可 |
| 寸法値（生値/補正値/手入力値） | 測点確定後に自動計算（生値）。基準物・対応点・幾何条件による補正で補正値を生成。利用者操作で手入力値を追加 | 対応する測点/セッションが存在すること | 補正ロジック実行のたびに補正値を再計算（生値は変更しない、確定=C-02）。手入力値は利用者操作でのみ更新 | 手入力値は削除して生値/補正値表示に戻せる【想定】 | 該当なし | 生値・補正値・手入力値は共存し、表示切替で参照する（上書きしない、確定=C-02） | 該当なし | セッション削除に連動 | 生値を常に保持するため、補正・手修正の妥当性を後から検証できる |
| 検証記録 | セッション完了時（または簡易2点間計測の完了時）に自動生成。実測値は利用者が別途入力 | 特になし（単一利用者） | 完了後も実測値・環境条件・メモ等を編集できる（確定） | 個別記録の削除が可能（確定）。削除は取り消し不可（Undo無し、確定） | 保持上限を設け、上限を超えた場合は古い記録から自動削除する（FIFO、確定）。具体的な上限値（件数または容量）は【要確認】 | 該当なし | 上限到達時は最も古い記録を自動的に削除してから新規記録を追加する（確定） | 端末内保存のため端末のストレージ容量に依存 | 削除後の復元機能は無し（Undo無し、確定） |
| 基準物設定 | 基準モード選択時に、登録済み基準物（初期はA4用紙）を1つまたは複数、異なる方向・位置に配置して撮影・自動検出する。または任意基準物の両端点を指定 | 該当セッション内でのみ有効【想定】 | 自動検出結果は利用者が輪郭・端点を修正してから確定（C-03） | 撮り直し・再指定が可能 | 1セッションにつき複数件配置可（同一種類の基準物を複数方向に設置して精度向上に用いる、確定）。上限件数は【要確認】 | 複数配置した基準物から得られる縮尺・幾何情報を、統合するか比較のみに使うかは【要確認】 | 該当なし | セッション終了で破棄 | 自動検出結果と利用者修正内容の両方を記録し、後から比較できる（確定） |

### 7.3 状態×権限マトリクス

該当なし（単一利用者・権限区分なし）。一般公開等で複数利用者対応を行う際に再検討する。

### 7.4 主要フロー完結性

| フロー | 開始条件 | 成功時の着地 | 戻る/取消 | 失敗時の案内と再試行 | 中断/再開 | 二重実行防止 |
|---|---|---|---|---|---|---|
| 部屋計測（4点／L字5点以上） | 対応環境チェック通過 → AR開始 | 四辺・面積・簡易平面図の表示、検証記録への保存 | 「1点戻る」、任意点削除、セッション破棄（最初から） | 非対応環境／床認識失敗／追跡喪失時に理由と対処法を案内し、再試行を促す | セッションを閉じての中断・再開は不要（1セッション完結、確定）。追跡ロスト時のみ直前測点を保持して基準取り直し後に再開 | 同時に複数セッションを開始できない【想定】 |
| 簡易2点間距離計測 | 簡易計測モードを選択 | 2点間距離の表示。検証記録として保存する（確定） | やり直し可能 | 部屋計測と同様に追跡喪失時の案内を行う | 該当なし（都度完結、確定） | 該当なし |
| バッチ撮影による対応点結合 | AR起動中でなくてもよい。通常カメラ等で撮影済みの複数枚の写真を用意（確定） | 対応点・既知距離指定後、WebXR測点との比較・補正に利用（A方式と併用、確定） | 対応点指定のやり直し可能 | 対応点不足・幾何条件不整合時は警告し再指定を促す | 中断・再開は不要。写真読み込みから対応点指定までを1回の操作で完結させる（確定） | 該当なし |
| 写真方式（D方式） | アプリ内撮影（傾きセンサー値を保存）、またはギャラリーから写真を取り込み | 同一辺の指定・傾き補正・縮尺付与を経て、寸法付きの平面図を表示し、検証記録へ保存 | 撮り直し、辺の指定やり直し、縮尺の再入力 | 写真間で同一辺が無い／傾きセンサー値が無い（取り込み写真）／目標物が写っていない場合は理由を案内し、鉛直線の手動指定や辺長入力へ誘導 | 中断・再開は不要（1回の操作で完結）。撮影した写真は端末内に保存される（確定、10章参照） | 該当なし |
| 折れ線入力 | 起点を置いて入力開始（写真方式の結果の辺から始めることも可、混在の詳細は【要確認】） | 入力した折れ線の寸法付き平面図を表示。閉じる指定がある場合は閉合誤差（ずれの距離・方向）を表示 | 直前の入力の取消、任意の行の編集・削除 | 数値不正・角度不正は入力欄で理由を示し修正を促す | 該当なし（都度完結） | 該当なし |

---

## 8. 機能要件

### 8.1 機能一覧

優先度は「核／検証／あれば／将来」で表す。**優先度は発案者の指示により今後変動しうる（確定）。**

| 機能ID | 機能名 | 目的 | 概要 | 優先度 |
|--------|--------|------|------|--------|
| F-001 | 対応環境チェック | 計測開始前に失敗を防ぐ | WebXR等が利用できるかを開始前に確認する | 核 |
| F-002 | AR開始・床の基準指定 | 位置推定の基準を定める | カメラと位置推定を開始し、測点の基準面を定める | 核 |
| F-003 | 4点の記録 | 長方形の採寸を行う | 長方形の角を順番に指定する | 核 |
| F-004 | 壁長・面積の計算 | 採寸結果を数値化する | 4点から四辺、周長、面積を計算する | 核 |
| F-005 | 簡易平面図 | 採寸結果を視覚化する | 取得点と寸法をSVG等で表示する | 核 |
| F-006 | 1点戻る・取り直す | 直近の誤操作を修正する | 誤って記録した直近の測点を取り消す | 核 |
| F-007 | 任意測点の選択削除・修正 | 直近以外の誤操作を修正する | 記録済みの任意の測点を選択して削除・修正する | 核 |
| F-008 | 精度検証記録 | 精度検証の証跡を残す | 実測値、計測値、差、成功・失敗条件を残す | 核 |
| F-009 | 重複画像の撮影（リアルタイム／バッチ） | 補助計測の入力を得る | 同じ角や特徴点が複数画像に入るように撮影する（AR起動中のリアルタイム撮影、またはAR起動なしでの事前撮影の両方に対応） | 検証 |
| F-010 | 対応点の手動指定 | 補助計測の対応関係を得る | 複数画像内の同一点を利用者が指定する | 検証 |
| F-011 | 基準距離の入力 | 縮尺の基準を得る | 実寸が分かる1区間を縮尺の基準として登録する | 検証 |
| F-012 | 基準モードの選択 | 状況に応じた縮尺付与を選べるようにする | 基準なし、メジャー、登録済み基準物、任意物から選ぶ | 検証 |
| F-013 | 基準物の輪郭候補検出 | 基準物の縮尺算出を補助する | 初期対応はA4用紙。基準物の四隅や外周を候補として表示する | 検証 |
| F-014 | 輪郭・端点の手動修正 | 誤検出をそのまま使わせない | 自動検出結果を利用者が必ず確認・修正してから確定する（C-03） | 検証 |
| F-015 | 幾何条件による比較・補正 | WebXR測点の妥当性を確認・改善する | 床平面、垂直壁、直角の全条件を用いてWebXR測点との整合性を確認する | 検証 |
| F-016 | 簡易2点間距離計測 | 部屋全体の計測を伴わない軽量な計測手段を提供する | 任意の2点間の距離のみを測る。縮尺基準モードを利用可能。精度目標・検証記録の対象に含める（確定） | 検証 |
| F-017 | 測点の安定性表示 | 不安定な計測を利用者に知らせる | 短時間のばらつきが大きい場合に静止・再試行を促す | あれば |
| F-018 | 寸法の手修正 | 自動計測を利用者が補える | 計測値を残したまま、補正後の値を別に保持する | あれば |
| F-019 | L字形などへの対応 | 長方形以外の部屋にも対応する | 5点以上の多角形を扱う | 将来 |
| F-020 | 保存・共有・出力 | 計測結果を持ち出せるようにする | 図面や測定履歴を端末またはオンラインに保存する | 将来 |
| F-021 | アプリ内撮影（傾きセンサー記録） | 写真方式の入力と傾き情報を同時に得る | アプリ内で複数枚撮影し、各写真に重力方向（ピッチ・ロール）等を保存する。写真方式の中核で、方式継続可否の鍵 | 検証（最優先） |
| F-022 | 写真の取り込み（ギャラリー） | 既存の写真でも検証できるようにする | ギャラリーから写真を取り込む。傾きはEXIF（焦点距離等）と鉛直線指定で補う。継続可否の判断を早めるため、F-021より後でも並行して作る | 検証 |
| F-023 | 傾き補正（センサー値／鉛直線指定） | 写真の傾きを補正し、平面上の寸法を出せるようにする | 傾きセンサー値、または写真内の鉛直な線を利用者が2本以上指定して傾きを補正する。目標物と測る辺は同じ高さの平面にある前提で、床面以外（高い位置）も条件付きで対象とする | 検証 |
| F-024 | 写真間の同一辺の指定 | 複数写真を1つの平面図につなぐ | 写真Aのこの辺と写真Bのこの辺は同じ、と利用者が指定する（1部屋3〜6枚想定）。F-010と関連 | 検証 |
| F-025 | 縮尺付与（辺長入力／目標物） | 写真上の長さを実寸に換算する | ①メジャーで測った辺長の入力、②各写真に写したA4等の同一目標物の指定、の順で優先（①があれば①を優先）。F-011・F-013・F-014と関連 | 検証 |
| F-026 | 折れ線入力（平板測量方式） | 写真のない部分の補完と、AR・写真に頼らない採寸手段を提供する | 「起点から右／左へ○度折れ（90度以外も可）、○○m」を順に入力して平面図化。閉じる指定時は閉合誤差（ずれの距離・方向）を表示（配分補正はしない）【想定】 | 検証 |

### 8.2 機能詳細

#### F-001 対応環境チェック
- 目的: WebXR非対応端末での計測失敗を未然に防ぐ
- 処理: WebXRおよびHit Test等の対応可否を起動時に判定し、非対応の場合は理由と代替手段を案内する【想定】
- 棄却条件: 【想定】対象端末がすべてAR対応を保証される状態になり、事前確認が形骸化した場合

#### F-002 AR開始・床の基準指定
- 目的: 以降の測点記録の基準となる座標系を定める
- 処理: カメラ映像から床平面を検出し、基準面として保持する
- 棄却条件: 【想定】WebXRを位置推定の主方式として採用しないと判断された場合（方式転換に相当）

#### F-003 4点の記録
- 目的: 長方形の部屋の採寸を行う
- 処理: 利用者が画面上で角を順に指定し、測点として記録する
- 棄却条件: 【想定】長方形の採寸自体を提供しないと判断された場合（本アプリの中核機能のため、棄却条件はサービスの方針転換に近い粒度になる）

#### F-004 壁長・面積の計算
- 目的: 記録した測点から実用的な数値を得る
- 処理: 隣接する測点間の3次元ユークリッド距離から辺長を、Shoelace公式から面積を算出する
- 棄却条件: 【想定】測点から寸法を計算する必要がなくなった場合（F-003と同様、中核機能）

#### F-005 簡易平面図
- 目的: 採寸結果を直感的に把握できるようにする
- 処理: 測点と寸法値をSVG等で表示する
- 棄却条件: 【想定】数値表示のみで十分と判断され図示が不要になった場合

#### F-006 1点戻る・取り直す
- 目的: 直近の誤操作を素早く修正する
- 処理: 直近1点の測点記録を取り消す
- 棄却条件: 【想定】誤操作率が実用上無視できるほど低いと判明した場合

#### F-007 任意測点の選択削除・修正
- 目的: 直近以外の測点の誤りにも対応する
- 処理: 記録済みの任意の測点を選択し、削除または再取得する
- 棄却条件: 【想定】F-006（1点戻る）のみで運用上支障がないと判明した場合

#### F-008 精度検証記録
- 目的: 精度検証の証跡を残し、改善の判断材料にする
- 処理: 測定回数・実測値・環境条件・端末情報・出力形式を記録する（8.1・8.2の各機能から棄却条件案参照）
- 棄却条件: 【想定】精度検証フェーズが終了し、実用運用フェーズへ移行した場合

#### F-009 重複画像の撮影（リアルタイム／バッチ）
- 目的: WebXR測点の検証・補正に使う画像を取得する
- 処理: リアルタイム方式ではAR中に向きを変えながら撮影する。バッチ方式ではAR起動中でなくてもよく、事前に撮影済みの複数枚の写真を後から読み込む（A方式と併用、確定）
- 棄却条件: 【想定】A方式（WebXR単独）だけで目標精度を安定して達成できると判明した場合

#### F-010 対応点の手動指定
- 目的: 複数画像間の位置関係を推定する材料を得る
- 処理: 利用者が画像上で同一の実世界の点を指定する
- 棄却条件: 【想定】F-009自体が不要になった場合に連動して不要になる

#### F-011 基準距離の入力
- 目的: 縮尺の実寸換算を可能にする
- 処理: 実寸が分かる1区間の両端を画像上で指定し、実寸値を入力する
- 棄却条件: 【想定】他の縮尺基準モードのみで十分な精度が得られると判明した場合

#### F-012 基準モードの選択
- 目的: 状況に応じた縮尺付与手段を選べるようにする
- 処理: 基準なし／メジャー／登録済み基準物／任意基準物から選択する
- 棄却条件: 【想定】いずれか1モードのみで精度・操作性が十分と判明した場合

#### F-013 基準物の輪郭候補検出
- 目的: 登録済み基準物からの縮尺付与を簡易化する
- 処理: 初期対応はA4用紙（ISO216）のみ。撮影画像から輪郭・四隅の候補を検出する。同一種類の基準物を複数方向・複数位置に配置して撮影し、精度向上に用いることができる（確定）。複数配置した基準物から得た縮尺・幾何情報の統合方法（平均化・比較のみ等）は【要確認】
- 棄却条件: 【想定】自動検出の精度・再現性が低く、手動指定（F-011/F-014）のみで十分と判明した場合

#### F-014 輪郭・端点の手動修正
- 目的: 自動検出の誤りをそのまま採寸結果に反映させない（C-03）
- 処理: 自動検出結果を画面に提示し、利用者がずれている点を修正してから確定する。確定操作を経ない検出結果は縮尺計算に使わない
- 棄却条件: 【想定】自動検出の精度が手動修正を要しないレベルに達したと十分な件数で確認された場合

#### F-015 幾何条件による比較・補正
- 目的: WebXR測点の妥当性を確認し、可能なら精度を改善する
- 処理: 床平面・垂直壁・直角の全条件を用いて、対応点・既知距離とあわせた比較・補正計算を行う
- 棄却条件: 【想定】幾何条件を用いた補正で誤差やばらつきの改善が確認できなかった場合

#### F-016 簡易2点間距離計測
- 目的: 部屋全体の計測を伴わずに、必要な距離だけを素早く知りたい場面に対応する
- 処理: 部屋計測セッションとは別に、任意の2点間の距離を測る。縮尺基準モード（基準なし／メジャー／登録済み基準物／任意基準物）を利用可能とする
- 棄却条件: 【想定】部屋計測セッション（F-003等）で代替でき、単独の2点間計測ニーズがないと判明した場合
- 備考: 精度目標（±3〜5cm目標・合否判定としない）・検証記録の対象に含める（確定）

#### F-017 測点の安定性表示
- 目的: 不安定な状態での計測を防ぐ
- 処理: 短時間の測点のばらつきが大きい場合に静止・再試行を促す
- 棄却条件: 【想定】WebXRの安定性が十分高く、警告表示が不要と判明した場合

#### F-018 寸法の手修正
- 目的: 自動計測の誤差を利用者が補えるようにする
- 処理: 計測値を残したまま、補正後の値を別に保持する（C-02と整合）
- 棄却条件: 【想定】自動計測の精度が手修正を要しないレベルに達した場合

#### F-019 L字形などへの対応
- 目的: 長方形以外の部屋形状にも対応する
- 処理: 5点以上の多角形として測点を扱う
- 棄却条件: 【想定】長方形以外の形状への需要が確認されない場合

#### F-020 保存・共有・出力
- 目的: 計測結果を端末外に持ち出せるようにする
- 処理: 図面や測定履歴を端末またはオンラインに保存する
- 棄却条件: 【想定】個人検証の範囲に留まり、共有・持ち出しのニーズが発生しない場合

#### F-021 アプリ内撮影（傾きセンサー記録）
- 目的: 傾き補正の入力（重力方向）を写真と同時に確実に得る
- 処理: アプリ内のカメラで撮影し、各写真にピッチ・ロール、撮影日時（UTC）、端末情報を紐付けて保持する。写真は解析用に縮小して端末内に保存する（10章参照。撮影と同時に縮小・グレースケール化し、元画像は保持しない）。EXIFは再エンコードで失われるため、焦点距離・元画像サイズ・傾きセンサー値は別に保存する
- 棄却条件: 【想定】傾きセンサー値で補正しても、鉛直線指定による補正より精度が改善しない場合

#### F-022 写真の取り込み（ギャラリー）
- 目的: 既存写真でも検証でき、方式継続可否の判断を早める
- 処理: 傾きセンサー値が無いため、焦点距離（EXIF）と鉛直線指定（F-023）で補う。EXIFに焦点距離が無い場合の扱いは基本設計で決める
- 棄却条件: 【想定】アプリ内撮影（F-021）のみで検証・運用が足りる場合

#### F-023 傾き補正（センサー値／鉛直線指定）
- 目的: 撮影角度による遠近の歪みを補正し、平面上の寸法を求める
- 処理: 傾きセンサー値がある写真はそれを使う。無い写真は、利用者が鉛直な線（壁の角・ドア枠等）を2本以上指定して鉛直方向の消失点を求める（半自動）。全自動の線検出は初期対象外【想定】
- 棄却条件: 【想定】補正をかけても補正なしと同程度の誤差にしかならない場合

#### F-024 写真間の同一辺の指定
- 目的: 複数写真を1つの平面図に統合する
- 処理: 写真Aの辺と写真Bの辺を「同一」として指定する。指定の不足・矛盾（長さの不一致等）は警告する
- 棄却条件: 【想定】1枚の写真だけで対象が収まり、統合が不要な場合

#### F-025 縮尺付与（辺長入力／目標物）
- 目的: 写真上の長さを実寸に換算する
- 処理: ①メジャーで測った辺長を入力（優先）、②各写真に同一の目標物（A4用紙等）を写し、四隅を利用者が確認・修正して確定（C-03）。目標物は測る辺と同じ高さの平面に置く前提。距離が離れるほど基準の読み取り誤差が拡大する点は、検証で誤差傾向として記録する
- 棄却条件: 【想定】辺長入力だけで十分な精度が得られ、目標物が不要な場合

#### F-026 折れ線入力（平板測量方式）
- 目的: 写真のない部分の補完、および他方式の精度が出ない場合の採寸手段
- 処理: 起点から右／左へ角度（90度以外も可）折れ、長さ（m）を順に入力して平面図化する。閉じる指定がある場合、閉じるために必要な差（距離・方向）を表示する（配分補正は行わない【想定】）。写真方式の辺との混在の可否は【要確認】（20章 No.12）
- 棄却条件: 【想定】写真方式・AR方式だけで補完が不要な場合、または手入力が現実的でないほど手間な場合

### 8.3 権限・ロール定義

該当なし（単一利用者・権限区分なし）。一般公開等で複数利用者に対応する際に再検討する。

### 8.4 帳票・画面一覧

【想定】（基本設計で確定。現時点の想定は次の通り）

| 画面ID | 名称 | 種別 | 備考 |
|--------|------|------|------|
| S-001 | 対応環境チェック画面 | 画面 | F-001 |
| S-002 | AR計測画面（4点／L字記録） | 画面 | F-002〜F-007 |
| S-003 | 基準モード選択・基準物撮影画面 | 画面 | F-011〜F-015 |
| S-004 | 結果表示画面（四辺・面積・簡易平面図） | 画面 | F-004・F-005 |
| S-005 | 検証記録一覧・詳細画面 | 画面 | F-008 |
| S-006 | 簡易2点間距離計測画面 | 画面 | F-016 |
| S-007 | バッチ撮影・対応点指定画面 | 画面 | F-009・F-010 |

### 8.5 UI表現の申し送り（フォント・アイコン・SIKUMILABの共通表示）

| 項目 | 内容 |
|------|------|
| 従うべき決まり | なし【想定】 |
| ロゴ・ファビコン・イラスト | 提供者・提供時期未定【要確認】 |
| 選択肢を狭める事情 | なし【想定】 |
| 書体・アイコンの決定 | 未定。基本設計でモックを比較して決定する（別指定がなければ商用利用可のフリー日本語フォントを候補にする） |

本案件はSIKUMILAB案件として、次のブランド表示標準を**採用**する（27番の回答による）。

| 項目 | 要件・対象画面 | 採否・変更内容・例外理由 |
|---|---|---|
| ヘッダ | アイコン/ロゴ＋サービス名「ハカルーム」。主ナビゲーションの要否は画面数が確定してから判断 | 採用【想定：詳細は基本設計】 |
| 詳細画面 | 戻り導線、タイトル、編集等の主要操作、補足説明を各画面に設置 | 採用【想定：詳細は基本設計】 |
| バージョン | フッタで現在版（`hakaroom-MAJOR.MINOR.PATCH`）を確認できるようにする | 採用 |
| コピーライト・フッタ | `© <年> SIKUMI LAB` をフッタに表示。年は年次更新 | 採用 |

---

## 9. 非機能要件

| 観点 | 要件 |
|------|------|
| 性能 | 同時利用者数1（単一利用者、確定）。4点記録から結果表示までの許容応答時間は【要確認】（20章 No.2） |
| 可用性 | 個人検証用途のため稼働時間帯・RTO/RPOの定めなし（該当なし、確定） |
| セキュリティ | 認証・認可の仕組みなし（単一利用者・ローカル利用、確定）。検証データは既定でサーバー送信・外部保存を行わない（確定＝C-01）。個人情報・機密情報は扱わない（部屋の映像・寸法のみ、確定） |
| 運用・保守 | 開発者本人が保守を兼任【想定】。詳細は14章 |
| 移植性・互換性 | 対応端末はPixel 6a（Android版Chrome）専用、PC非対応（確定）。iPhone対応は対象外（確定） |
| ユーザビリティ | 特筆要件なし（未定） |
| 国際化 | 該当なし（国内・日本語のみ）【想定】 |
| 正確性・準拠基準 | 距離計算＝3次元ユークリッド距離（確定）。面積計算＝Shoelace公式（確定）。単位＝メートル、表示はcm単位で四捨五入（確定）。精度目標＝壁1辺につき実測値との差±3〜5cm程度を目標とする。目標値であり、合否判定・必須合格条件・保証値ではない（確定。0.3で「±3cm以内」から表現修正）。検証方法＝実測値（メジャー等）と突き合わせ、誤差（cm・％）・平均・ばらつき・最大誤差・失敗条件を記録し、合否バッジは付けず傾向を読む（確定）。実用上の合否基準・対象距離環境・成功率は検証結果を踏まえ別途決定【要確認】（20章 No.1） |

---

## 10. データ要件

- 主要データ: 測点、計測セッション、寸法値（生値/補正値/手入力値）、検証記録、基準物設定（7.2参照）
- **写真（写真方式）**: 端末内（IndexedDB）に保存する（確定。サーバー送信なし=C-01）。解析用に縮小した画像のみ保存し、既定は長辺1600px・グレースケール・WebP品質0.6（1枚約100〜200KB、1部屋6枚で約1MB）。検証中は長辺・品質を設定で変更できる。人が見て判別できなくても、再解析に使えればよい。写真の合計上限は200MBで、超えたら古い写真から削除する（測定値の記録は残す）。永続ストレージ（`navigator.storage.persist()`）を要求する
- データ量: 測点・寸法値には件数上限なし（確定）。検証記録は保持上限を設け、上限を超えたら古い記録から自動削除する（FIFO、確定）。具体的な上限値（件数または容量）は【要確認】（20章 No.10）
- 保持期間: 端末内保存（localStorage等）。検証記録以外（測点・寸法値等）は各セッションに従属し、セッション終了時に検証記録へ集約された後は個別の保持期間を設けない【想定】

---

## 11. 外部インターフェース・システム連携

該当なし。現時点で外部システム・外部サービスとの連携は想定していない（確定）。

---

## 12. 稼働環境・システム構成

| 項目 | 内容 |
|------|------|
| ホスティング | GitHub Pages（初期採用、確定）。静的Webアプリ（サーバー処理なし）のため、いずれの候補でもHTTPS配信が可能で、後から他候補への移行も容易 |
| 既存インフラ | 該当なし |
| ネットワーク | インターネット経由のHTTPSアクセス、社外（モバイル）利用前提（確定） |
| 環境 | 検証用ページ1つのみ。開発／検証／本番の環境分離は無し【想定】 |

---

## 13. 移行要件

**省略**。既存システムからの移行を伴わない新規構築のため対象外。

---

## 14. 運用・保守要件

| 項目 | 内容 |
|------|------|
| 運用主体 | 開発者本人（シクミラボ）が兼任【想定】 |
| SLA | 個人検証のため定めなし（確定） |
| 問い合わせ・エスカレーション | 該当なし（単一利用者） |
| 保守契約 | 該当なし |
| リリース・変更管理 | 随時、開発者本人の判断で更新【想定】 |

---

## 15. 制約条件・前提条件

- **費用**: 初期費用・運用費ともに特になし（確定）
- **納期**: 特になし（確定）
- **利用必須の技術/製品**: WebXR、Android版Chrome、Pixel 6a、HTTPS（確定）
- **法規制・社内規程**: 該当なし（確定）
- **前提**: Google Play開発者サービス（AR）が利用可能であること、カメラ等のブラウザ権限が許可されること

### 15.1 開発標準提案と例外

| 項目 | 標準提案 | 採否 | 変更・例外・理由 |
|------|----------|------|------------------|
| バージョン管理 | `hakaroom-MAJOR.MINOR.PATCH`。PATCH=小修正、MINOR=後方互換のある機能追加、MAJOR=破壊的・大規模変更 | 採用 | — |
| 日付・時刻入力 | テキストボックス＋ピッカー等の標準操作 | 非該当 | 検証記録の日時は自動記録のみで、手入力欄は無い（確定） |
| タイムゾーン・ロケール | 保存・API・ログはUTC正規化、表示・入力は端末タイムゾーンに追従（取得不可・不定ならJSTへフォールバック） | 採用 | — |
| Web対応端末 | PC・スマホの主要機能を両対応 | 変更して採用 | 現段階はPixel 6a（Androidスマホ）専用、PC非対応（確定）。将来公開時にPC対応を再検討 |
| 画像表示 | 元画像の縦横比を維持し、引き伸ばさない | 採用 | 対象は基準物撮影画像・簡易平面図 |
| Web共有機能 | 共有ボタンを設け、対応環境では端末の共有機能、非対応時はURLコピーを使う | 将来検討 | F-020（保存・共有・出力）の一部として後続で仕様決定 |
| SIKUMILABのバージョン・コピーライト表示 | フッタに現在版と `© <年> SIKUMI LAB` を表示 | 採用 | — |
| SIKUMILABのヘッダ/フッタ共通構成 | アイコン/ロゴ＋サービス名＋主ナビゲーション等 | 採用 | 対象画面・詳細は基本設計で調整【想定】 |

### 15.2 禁止事項・制約を変更する場合

0章のA・B・Cの運用ルールを適用する。旧記述との衝突だけで発案者の指示を拒否しない。

---

## 16. スケジュール・マイルストーン

| マイルストーン | 予定時期 |
|----------------|----------|
| 要件確定 | 2026-09-23（本書） |
| 技術検証（2点間精度確認） | 未定【要確認】 |
| 技術検証（4点計測・A/B/C比較） | 未定【要確認】 |
| 実用可否判断 | 検証結果を踏まえて決定 |

たたき台にある通り、技術検証は「2点間計測 → 既知距離との比較 → 4点計測 → 補正ロジックの比較」の順に進める想定（確定）。目標未達だけで検証を中止せず、改善可能性を確認する（確定）。

---

## 17. 成功基準・受入基準

### 受入基準

- 長方形の部屋で4点計測を実行し、四辺の長さ・面積・簡易平面図が表示され、実測値との差を記録できること（確定）
- 検証記録に測定回数・実測値・環境条件・端末情報・出力形式が残ること（確定）
- WebXR測点方式（A）、固定地点重複撮影方式（B）、ハイブリッド方式（C）を同一の部屋・基準距離・実測値で比較し、辺誤差、角度、対角線、面積、閉合ずれ、操作時間、やり直し回数を記録できること（確定）

### 受入テストの想定

- 実施主体: 開発者本人（確定）
- 期間・環境: 技術検証計画に準じる

### 成功指標（KPI）

- 壁1辺につき実測値との差±3〜5cm程度を目標とするが、目標値であり合否判定・必須合格条件・保証値ではない。個人検証として、スマートフォンでできること・できないことを調べるのが意図（確定）。簡易2点間距離計測（F-016）も同じ精度目標・検証記録の対象に含める（確定）
- 実用上の合否基準（許容誤差・対象距離環境・成功率）は検証結果を踏まえて別途決定【要確認】（20章 No.1）

---

## 18. 調達・発注方針

**省略**。ベンダー選定を伴わない個人開発のため対象外。

---

## 19. リスクと課題

| リスク/課題 | 影響 | 対応方針 |
|-------------|------|----------|
| Pixel 6aがARCore対応でも、WebXR上で必要な機能・精度を満たすとは限らない | 計測方式（A案）の実現可否に直結 | 技術検証を最優先で実施し、達成できない場合はB/C方式や補正ロジックの改善を検討する |
| 模様が少ない床・壁、暗所、急な動きで位置推定が不安定になる可能性 | 精度低下・計測失敗 | F-017（測点の安定性表示）で利用者に静止・再試行を促す |
| 部屋を一周する間に位置推定のずれが積み重なり、始点と終点が合わない可能性 | 閉合誤差の発生 | F-015（幾何条件による比較・補正）で検出・補正を試みる |
| 固定地点で向きだけを変えた撮影では奥行きを決める視差が少ない | B方式（完全固定）だけでは部屋全体を一意に復元できない可能性 | 幾何条件（床平面・垂直壁・直角）と既知距離を追加して検証する |
| 対応点の手動指定における数ピクセルのずれが遠距離推定誤差を拡大させる可能性 | 補正精度の低下 | 検証記録で誤差の傾向を記録し、許容範囲を見極める |
| 基準物と測定対象が異なる平面・奥行きにある場合、誤った縮尺になる | 精度低下 | F-014で警告・再撮影誘導を行う（確定：③再撮影を促してブロック） |
| 角が家具で隠れている部屋では、計測が完了できない可能性 | 主要フローの行き止まり | 初期版では対象外として扱う（確定） |
| 写真方式で、ブラウザから傾きセンサー（DeviceOrientation）が十分な精度・安定性で取れるか不明 | 傾き補正が成立せず、写真方式の継続可否に直結 | F-021を最優先で検証し、取れない場合は鉛直線指定（F-023）で代替できるかを比べる |
| 目標物（A4）は小さく、遠い辺へ縮尺を拡大すると読み取り誤差が拡大する | 長い辺で誤差が大きくなる | 辺長入力を優先し、目標物のみの場合の誤差傾向を記録する |
| レンズ歪み（特に超広角）や、EXIF焦点距離の欠落・デジタルズームの影響 | 補正精度の低下 | 標準レンズ相当で撮影する前提とし、影響を検証で確認する |
| ±3〜5cm程度は精度目標であり、達成できる対象距離・環境、許容失敗率、実用上の基準は未確定 | 実用化判断が遅れる可能性 | 検証結果を踏まえ20章No.1の要確認事項を解消する |

---

## 20. 要確認事項

| No | 確認事項 | 確認先/決定者 | 期限 | 次に決める工程 | 設計影響 |
|----|----------|---------------|------|----------------|----------|
| 1 | 実用上の許容誤差・合否基準、対象距離・環境、成功率、計測失敗の定義（精度は±3〜5cm程度の目標・合否判定としない、は確定） | 開発者本人（検証結果を踏まえて） | 検証完了後 | 技術検証 | 後で可 |
| 2 | 4点記録から結果表示までの許容応答時間 | 開発者本人 | 基本設計着手前 | 基本設計 | 後で可 |
| 3 | WebXR Hit Test／AnchorsのPixel 6a上での利用可否・挙動 | 技術検証 | 実装着手前 | 技術検証 | 設計ブロッカー（計測方式の実現可否に直結） |
| 4 | 家具で角が隠れる場合、利用者にどう案内するか（対象外の伝え方） | 開発者本人 | 基本設計 | 基本設計 | 後で可 |
| 5 | 基準物と対象が非同一平面の場合の警告文言・再撮影誘導のUI詳細 | 開発者本人 | 基本設計 | 基本設計 | 後で可 |
| 6 | 想定利用者（開発者本人以外）への聞き取りは未実施 | 開発者本人 | 一般公開検討時 | 将来フェーズ | 後で可 |
| 7 | 一般公開時に必要な非機能要件（同時アクセス、複数利用者のデータ分離等） | 開発者本人 | 一般公開検討時 | 将来フェーズ・本書の改訂 | 後で可 |
| 8 | SIKUMILABヘッダ/フッタの対象画面・具体的表示内容 | 開発者本人／基本設計 | 基本設計 | 基本設計 | 後で可 |
| 9 | 複数配置した同一種類の基準物（7.2）から得られる縮尺・幾何情報を、統合するか比較のみに使うか。配置数の上限 | 開発者本人 | F-013/F-015詳細設計前 | 基本設計 | 後で可 |
| 10 | 検証記録の保持上限の具体的な値（件数または容量） | 開発者本人 | 基本設計 | 基本設計 | 後で可 |
| 12 | 折れ線入力と写真方式の辺を、1つの図面で混在させられるか（混在の仕方・起点の引き継ぎ） | 開発者本人 | F-026詳細設計前 | 基本設計 | 後で可 |
| 13 | 目標物と同じ高さの平面であれば床面以外（壁の高い位置）も測れる前提の、適用条件と検証範囲 | 開発者本人 | 写真方式の検証後 | 技術検証 | 後で可 |

**このドラフトレビューで解消した項目**（旧No.3・4・9・10・11・12・13相当）：ホスティング先＝GitHub Pages初期採用、F-016は検証記録対象に含める、バッチ撮影は中断・再開不要、Undo機能なし・削除は取り消し不可、検証記録は編集・個別削除可、基準物は複数配置可（統合方法は上記No.9へ）、データ保持は上限到達時に自動削除する方針（具体値は上記No.10へ）。v0.4で解消：写真本体は端末内に保存する（旧No.11、10章参照。欠番として再利用しない）。ロゴ・アイコン提供時期は要確認事項に積み上げず、8.5の「未定」表記のまま基本設計へ送る（document-template.mdの方針に従い削除）。

---

## 補足：本書における【想定】の扱いについて

本文中の【想定】は、AIが常識・文脈から仮置きした内容です。ドラフトレビューの際に、内容が正しいかご確認ください。誤りがあれば修正します。
